name: educational_products_app
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  uuid: ^4.4.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.1
  sqflite: ^2.4.2
  path: ^1.9.1
  supabase_flutter: ^2.15.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
